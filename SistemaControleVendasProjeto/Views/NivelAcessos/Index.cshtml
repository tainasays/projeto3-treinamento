@model IEnumerable<SistemaControleVendasProjeto.Models.NivelAcesso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoAcesso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoAcesso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NivelAcessoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NivelAcessoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NivelAcessoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
